# Read after Expo sets SKIP_BUNDLING=1 for Debug. Unset so "Bundle React Native code and images"
# runs for Debug *device* builds: `main.jsbundle` is embedded and the app works without Metro.
# (Debug simulator still skips bundling in react-native-xcode.sh and uses Metro when developing.)
unset SKIP_BUNDLING
